---
# oracle-linux-base.yml — OS baseline applied to all application VMs.
# Prerequisite: kvm-vms.yml has confirmed connectivity and OS version.

- name: Apply Oracle Linux OS baseline
  hosts: app_servers
  gather_facts: true
  roles:
    - role: oracle-linux-base
